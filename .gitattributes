# Preserve evidence bytes: Python csv writes RFC4180 CRLF; hashes cover exact bytes.
*.csv -text whitespace=cr-at-eol
*.log -text -whitespace
# Keep binary evidence out of textual diffs.
*.png binary
*.zip binary
*.pbf binary
